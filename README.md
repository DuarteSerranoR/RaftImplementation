# Raft Implementation
Project for Fault Tolerance in Distributed Systems - Lib developed for learning.

For the database:
    -driver -> PostgreSQL
    -server -> localhost
    -dbname -> raft-logs
    -uname -> dev
    -pwd -> passwd
